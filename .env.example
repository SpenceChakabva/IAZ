# Canonical origin used for absolute URLs in metadata, OpenGraph tags,
# the sitemap and robots.txt. No trailing slash.
#
# On Vercel this is optional — VERCEL_PROJECT_PRODUCTION_URL is used
# automatically. Set this only to pin a custom domain once one is live.
NEXT_PUBLIC_SITE_URL=https://iaz-omega.vercel.app
